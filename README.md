
```
show-direnv-diff X.Y.Z

USAGE:
    show-direnv-diff [OPTIONS]

FLAGS:
    -h, --help       Prints help information
    -V, --version    Prints version information

OPTIONS:
    -f <fmt>        Output format [default: direnv]  [possible values: direnv, json]
```